#include<iostream>
#include<algorithm>
using namespace std;

int main(){
    int a, b, c, x, y;
    cin>>a>>b>>c>>x>>y;
    int ans = 2*(5000*10**5);
    int total;
    for (int i = 1; i < 10**5+1; i++){
        total = a*max(X-Z, 0) + b*max(Y-Z, 0) + 2*C*Z;
        ans = min(total, ans)
    }
    cout<<ans<<endl;
}